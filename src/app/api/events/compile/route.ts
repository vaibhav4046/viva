/**
 * Kept as the old address of the turn endpoint. One implementation lives in
 * `/api/study/turn`; this alias exists only so the study screen keeps working
 * while it moves over. Delete once nothing posts here.
 */
export { POST } from "@/app/api/study/turn/route";
